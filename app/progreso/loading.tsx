import { ScreenLoading } from '@/app/_components/ScreenLoading';

// Shown at once while Progreso loads (see ScreenLoading).
export default function ProgresoLoading() {
  return <ScreenLoading kind="progreso" />;
}
