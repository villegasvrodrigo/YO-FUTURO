import { ScreenLoading } from '@/app/_components/ScreenLoading';

// Shown at once while Inicio loads (see ScreenLoading).
export default function InicioLoading() {
  return <ScreenLoading kind="inicio" />;
}
