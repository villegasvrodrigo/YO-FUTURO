import { ScreenLoading } from '@/app/_components/ScreenLoading';

// Shown at once while Perfil loads (see ScreenLoading).
export default function PerfilLoading() {
  return <ScreenLoading kind="perfil" />;
}
